<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fondo -->
    <solid android:color="@color/zxing_transparent"/>

    <!-- Bordes redondeados -->
    <corners android:radius="30dp"/>

    <!-- Borde -->
    <stroke
        android:width="2dp"
        android:color="@color/azul_profundo"/>

</shape>